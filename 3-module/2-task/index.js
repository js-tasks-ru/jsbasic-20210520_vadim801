function filterRange(arr, a, b) {
  return arr.filter(el => (el >= a && el <= b));
}


/*function filterRange(arr, a, b) {
  var num = [];
 // console.log(arr.[0])
  //console.log(a)
  for (let i = 0; i < arr.length; i++){
    if ( arr.[i] >= a && arr.[i] <= b){
       num.push(arr.[i])
    }
  }
  return num;
    // Ваш код
}*/